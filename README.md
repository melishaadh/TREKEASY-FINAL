# TREKEASY-FINAL
# 1. Prerequisites
node --version   # needs 18+
npm --version

# 2. Install Expo CLI globally (once)
npm install -g expo-cli

# 3. Install all project dependencies
npm install

# 4. Start dev server
npm run dev
