FOR YOU 

import React from 'react';
import { 
  View, 
  Text, 
  ScrollView, 
  TextInput, 
  Image, 
  TouchableOpacity, 
  SafeAreaView, 
  Dimensions, 
  StatusBar,
  Platform,
  StyleSheet
} from 'react-native';
import { 
  Menu, 
  Search, 
  Filter, 
  Compass, 
  Heart, 
  Map as MapIcon, 
  User, 
  Calendar, 
  ChevronLeft, 
  MessageSquare,
  LogOut,
  LogIn,
  TrendingUp,
  Award
} from 'lucide-react-native';
import { styled } from 'nativewind';

// --- Styled Components ---
const StyledView = styled(View);
const StyledText = styled(Text);
const StyledScrollView = styled(ScrollView);
const StyledImage = styled(Image);
const StyledTouchableOpacity = styled(TouchableOpacity);

const { width } = Dimensions.get('window');

// --- Types ---
interface Trek {
  id: string;
  parentName: string;
  displayTitle: string;
  priceNPR: number;
  durationDays: number;
  description: string;
  rating: number;
  elevation: string;
  distance: string;
  difficulty: string;
  image: string;
  tag?: string;
}

// --- Mock Data ---
const TREKS: Trek[] = [
  { 
    id: '1', 
    parentName: 'Patagonia', 
    displayTitle: 'Mountain Crest Ridge', 
    priceNPR: 85000, 
    durationDays: 10, 
    description: 'A classic journey...', 
    rating: 4.9,
    elevation: '680m',
    distance: '12.4 km',
    difficulty: 'Hard',
    tag: 'PRO',
    image: 'https://images.unsplash.com/photo-1544735716-392fe2489ffa?q=80&w=800' 
  },
  { 
    id: '8', 
    parentName: 'Alps', 
    displayTitle: 'Azure Lake Perimeter', 
    priceNPR: 150000, 
    durationDays: 12, 
    description: '...', 
    rating: 4.8,
    elevation: '340m',
    distance: '8.2 km',
    difficulty: 'Moderate',
    tag: 'HOT THIS WEEK',
    image: 'https://images.unsplash.com/photo-1533130061792-64b345e4a833?q=80&w=800' 
  }
];

// --- Shared Components ---

const GlassFooter = ({ activeTab = 'For You' }) => (
  <StyledView 
    className="absolute bottom-0 left-0 right-0 flex-row justify-around items-center py-4 bg-white/90 border-t border-gray-100 pb-safe"
    style={styles.glassEffect}
  >
    {[
      { name: 'Explore', icon: Compass },
      { name: 'Saved', icon: Heart }, 
      { name: 'For You', icon: Award },
      { name: 'My Treks', icon: MapIcon },
      { name: 'Profile', icon: User },
    ].map((tab) => (
      <StyledTouchableOpacity key={tab.name} className="items-center">
        <tab.icon size={24} color={activeTab === tab.name ? "#2d6a4f" : "#666"} />
        <StyledText className={`text-[10px] mt-1 ${activeTab === tab.name ? "text-[#2d6a4f] font-bold" : "text-[#666]"}`}>
          {tab.name}
        </StyledText>
      </StyledTouchableOpacity>
    ))}
  </StyledView>
);

const RecommendationCard = ({ trek }: { trek: Trek }) => (
  <StyledView className="w-72 mr-4 bg-white rounded-3xl overflow-hidden border border-gray-100 shadow-sm">
    <StyledView className="relative">
      <StyledImage source={{ uri: trek.image }} className="w-full h-44" />
      {trek.tag && (
        <StyledView className="absolute top-3 right-3 bg-[#1d3557]/80 px-2 py-1 rounded-md">
          <StyledText className="text-white text-[10px] font-bold">{trek.tag}</StyledText>
        </StyledView>
      )}
    </StyledView>
    <StyledView className="p-4">
      <StyledText className="text-xl font-bold text-gray-900">{trek.displayTitle}</StyledText>
      <StyledView className="flex-row items-center mt-2 opacity-60">
        <MapIcon size={14} color="#666" />
        <StyledText className="text-xs ml-1">{trek.distance} • {trek.difficulty}</StyledText>
      </StyledView>
      <StyledView className="flex-row justify-between items-center mt-4">
        <StyledView className="flex-row items-center">
          <StyledText className="text-lg font-bold text-gray-900">{trek.rating}</StyledText>
          <Heart size={16} color="#2d6a4f" fill="#2d6a4f" className="ml-1" />
        </StyledView>
        <StyledText className="text-xs text-gray-400 font-medium">{trek.elevation} Elev.</StyledText>
      </StyledView>
    </StyledView>
  </StyledView>
);

// --- Main Screen ---

export const ForYouScreen = () => {
  return (
    <SafeAreaView className="flex-1 bg-white">
      <StatusBar barStyle="dark-content" />
      <StyledScrollView 
        className="flex-1" 
        showsVerticalScrollIndicator={false} 
        contentContainerStyle={{ paddingBottom: 120 }}
      >
        {/* Header - Aligned with Dashboard */}
        <StyledView className="px-6 py-4 flex-row justify-between items-center">
          <StyledView className="w-10 h-10 bg-gray-900 rounded-xl items-center justify-center">
            <StyledText className="text-white font-bold text-xs">TE</StyledText>
          </StyledView>
          <StyledText className="text-2xl font-bold text-[#2d6a4f]">TrekEasy</StyledText>
          <StyledView className="flex-row items-center">
            <Search size={24} color="#333" />
            <StyledView className="ml-4 w-10 h-10 rounded-full overflow-hidden border-2 border-gray-100">
              <StyledImage 
                source={{ uri: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?q=80&w=200' }} 
                className="w-full h-full" 
              />
            </StyledView>
          </StyledView>
        </StyledView>

        {/* Hero Section */}
        <StyledView className="px-6 pt-6 pb-4">
          <StyledText className="text-xs text-gray-500 font-bold uppercase tracking-widest mb-1">Welcome Back, Alex</StyledText>
          <StyledText className="text-4xl font-bold text-gray-900 leading-tight">Curated for your next adventure</StyledText>
        </StyledView>

        {/* Based on Interest */}
        <StyledView className="mt-8">
          <StyledView className="px-6 flex-row justify-between items-center mb-4">
            <StyledText className="text-2xl font-bold text-gray-900">Based on your interest</StyledText>
            <StyledTouchableOpacity>
              <StyledText className="text-[#2d6a4f] font-bold text-xs uppercase tracking-wider">View All</StyledText>
            </StyledTouchableOpacity>
          </StyledView>
          <StyledScrollView horizontal showsHorizontalScrollIndicator={false} className="pl-6">
            {TREKS.map(trek => (
              <RecommendationCard key={trek.id} trek={trek} />
            ))}
          </StyledScrollView>
        </StyledView>

        {/* Trending in Region */}
        <StyledView className="mt-10">
          <StyledView className="px-6 flex-row justify-between items-center mb-4">
            <StyledText className="text-2xl font-bold text-gray-900">Trending in your region</StyledText>
            <StyledTouchableOpacity>
              <StyledText className="text-[#2d6a4f] font-bold text-xs uppercase tracking-wider">Explore Map</StyledText>
            </StyledTouchableOpacity>
          </StyledView>
          <StyledScrollView horizontal showsHorizontalScrollIndicator={false} className="pl-6">
            {TREKS.map(trek => (
              <RecommendationCard key={trek.id} trek={{...trek, tag: 'HOT THIS WEEK'}} />
            ))}
          </StyledScrollView>
        </StyledView>

        {/* Gamification / Progress Card */}
        <StyledView className="px-6 mt-10">
          <StyledView className="bg-[#e9f0ed] p-6 rounded-[32px] border border-[#d1e0d9]">
            <StyledView className="flex-row items-start">
              <TrendingUp size={28} color="#2d6a4f" />
              <StyledView className="ml-4 flex-1">
                <StyledText className="text-2xl font-bold text-[#1d3557]">Ready to level up?</StyledText>
                <StyledText className="text-gray-600 mt-1 leading-5">You've completed 3 moderate treks this month.</StyledText>
              </StyledView>
            </StyledView>
            <StyledView className="mt-6 h-2 w-full bg-white rounded-full overflow-hidden">
              <StyledView className="h-full bg-[#2d6a4f] w-[70%]" />
            </StyledView>
            <StyledText className="mt-4 text-[10px] text-center font-bold text-[#1d3557] uppercase tracking-[2px]">
              1 Trek away from 'Intermediate' Badge
            </StyledText>
          </StyledView>
        </StyledView>
      </StyledScrollView>
      <GlassFooter />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  glassEffect: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: -10 },
    shadowOpacity: 0.05,
    shadowRadius: 15,
    elevation: 10,
    ...Platform.select({
      ios: { backgroundColor: 'rgba(255, 255, 255, 0.92)' },
      android: { backgroundColor: '#ffffff' }
    })
  }
});

export default ForYouScreen;